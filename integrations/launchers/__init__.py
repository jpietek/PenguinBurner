"""What every game launcher looks like once the library tab has to show it.

Steam and Lutris disagree about nearly everything around a game -- where it is
installed, what identifies it, how a wrapper gets in front of it -- but the
library view needs the same handful of facts from both. This package holds that
handful and nothing else; each launcher's own code stays in its own directory.
"""
